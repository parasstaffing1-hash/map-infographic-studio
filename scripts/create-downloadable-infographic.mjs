import { readFile, writeFile } from 'node:fs/promises';

const width = 1080;
const height = 1350;
const background = '#fbf8ef';
const ink = '#173b34';
const muted = '#5e736c';
const green = '#147d6e';
const lightGreen = '#d8efe1';
const sourceUrl = 'https://cdnbbsr.s3waas.gov.in/s3716e1b8c6cd17b771da77391355749f3/uploads/2026/04/20260415955675604.pdf';
const values = [
  ['Andhra Pradesh', 15.98], ['Arunachal Pradesh', 2.02], ['Assam', .96], ['Bihar', .65], ['Chhattisgarh', 2.32], ['Goa', .08], ['Gujarat', 47.18], ['Haryana', 3.01], ['Himachal Pradesh', 12.80], ['Jammu and Kashmir', 3.63], ['Jharkhand', .49], ['Karnataka', 26.72], ['Kerala', 4.57], ['Ladakh', .15], ['Madhya Pradesh', 12.18], ['Maharashtra', 31.98], ['Manipur', .13], ['Meghalaya', .40], ['Mizoram', .14], ['Nagaland', .11], ['Odisha', 3.24], ['Punjab', 3.44], ['Rajasthan', 47.02], ['Sikkim', 2.34], ['Tamil Nadu', 29.11], ['Telangana', 7.93], ['Tripura', .05], ['Uttar Pradesh', 7.01], ['Uttarakhand', 6.02], ['West Bengal', 2.11], ['Andaman and Nicobar Islands', .04], ['Chandigarh', .08], ['Dadra and Nagar Haveli and Daman and Diu', .14], ['Delhi', .51], ['Lakshadweep', .01], ['Puducherry', .08],
].map(([region, value]) => ({ region, value }));
const rows = values.sort((a, b) => b.value - a.value);
const outline = JSON.parse(await readFile(new URL('../public/data/india_official.json', import.meta.url), 'utf8'));

const flatten = (value, out = []) => { if (Array.isArray(value)) { if (typeof value[0] === 'number') out.push(value); else value.forEach((item) => flatten(item, out)); } return out; };
const coordinates = flatten(outline.features.flatMap((feature) => feature.geometry.coordinates));
const minLon = Math.min(...coordinates.map((pair) => pair[0]));
const maxLon = Math.max(...coordinates.map((pair) => pair[0]));
const minLat = Math.min(...coordinates.map((pair) => pair[1]));
const maxLat = Math.max(...coordinates.map((pair) => pair[1]));
const project = ([lon, lat]) => [92 + ((lon - minLon) / (maxLon - minLon)) * 428, 1050 - ((lat - minLat) / (maxLat - minLat)) * 700];
const pathForCoordinates = (coords) => coords.map((ring) => { const points = ring.map(project); return `M${points.map(([x, y]) => `${x.toFixed(1)},${y.toFixed(1)}`).join('L')}Z`; }).join('');
const indiaPath = outline.features.flatMap((feature) => feature.geometry.coordinates).map((polygon) => pathForCoordinates(polygon)).join('');
const points = { Gujarat: [71.7, 22.3], Rajasthan: [74.8, 27.0], Karnataka: [76.0, 15.2], 'Tamil Nadu': [78.4, 11.0], Maharashtra: [75.3, 19.2], 'Andhra Pradesh': [79.3, 15.8], 'Madhya Pradesh': [78.3, 23.7], 'Himachal Pradesh': [77.3, 31.8], Telangana: [79.2, 18.0], Kerala: [76.4, 10.3], Odisha: [85.5, 20.4], 'Uttar Pradesh': [80.8, 26.8] };
const esc = (value) => String(value).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
const max = rows[0].value;
const topRows = rows.slice(0, 8);
const bars = topRows.map((row, index) => { const y = 462 + index * 54; const barWidth = Math.round((row.value / max) * 290); return `<g><text x="628" y="${y}" class="bar-label">${esc(row.region)}</text><rect x="628" y="${y + 12}" width="290" height="11" rx="5.5" fill="#e2eee8"/><rect x="628" y="${y + 12}" width="${barWidth}" height="11" rx="5.5" fill="${index < 2 ? green : '#4aa98b'}"/><text x="934" y="${y + 22}" text-anchor="end" class="bar-value">${row.value.toFixed(2)} GW</text></g>`; }).join('');
const bubbles = Object.entries(points).map(([name, coords]) => { const row = rows.find((item) => item.region === name); if (!row) return ''; const [x, y] = project(coords); const radius = 8 + Math.sqrt(row.value / max) * 20; return `<g><circle cx="${x.toFixed(1)}" cy="${y.toFixed(1)}" r="${radius.toFixed(1)}" fill="#147d6e" fill-opacity=".78" stroke="#fbf8ef" stroke-width="3"/><text x="${(x + radius + 7).toFixed(1)}" y="${(y + 4).toFixed(1)}" class="map-label">${esc(name)} · ${row.value.toFixed(1)}</text></g>`; }).join('');
const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="${width}" height="${height}" viewBox="0 0 ${width} ${height}"><defs><linearGradient id="paper" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#fffdf7"/><stop offset="1" stop-color="#f3f8f1"/></linearGradient><filter id="shadow" x="-20%" y="-20%" width="140%" height="140%"><feDropShadow dx="0" dy="7" stdDeviation="10" flood-color="#173b34" flood-opacity=".10"/></filter><style>.kicker{font:700 16px Inter,Arial,sans-serif;letter-spacing:2.4px;fill:${green}}.title{font:800 52px Inter,Arial,sans-serif;fill:${ink};letter-spacing:-1.8px}.subtitle{font:400 19px Inter,Arial,sans-serif;fill:${muted}}.section{font:800 14px Inter,Arial,sans-serif;letter-spacing:1.6px;fill:${green}}.bar-label{font:600 16px Inter,Arial,sans-serif;fill:${ink}}.bar-value{font:700 15px Inter,Arial,sans-serif;fill:${green}}.map-label{font:600 11px Inter,Arial,sans-serif;fill:${ink}}.small{font:400 13px Inter,Arial,sans-serif;fill:${muted}}.metric{font:800 26px Inter,Arial,sans-serif;fill:${ink}}.metric-label{font:600 12px Inter,Arial,sans-serif;fill:${muted};letter-spacing:.4px}</style></defs><rect width="${width}" height="${height}" fill="url(#paper)"/><rect x="44" y="44" width="992" height="1262" rx="24" fill="none" stroke="#d6e7dc" stroke-width="2"/><text x="84" y="105" class="kicker">INDIA · RENEWABLE POWER · 31 MAR 2026</text><text x="84" y="178" class="title">Where India’s clean power lives</text><text x="84" y="218" class="subtitle">Installed renewable capacity by state and union territory, converted from MW to GW.</text><line x1="84" y1="258" x2="996" y2="258" stroke="#cfe2d7" stroke-width="2"/><g filter="url(#shadow)"><rect x="84" y="288" width="438" height="72" rx="16" fill="#ffffff"/><rect x="544" y="288" width="452" height="72" rx="16" fill="#ffffff"/></g><text x="112" y="321" class="metric">47.18 GW</text><text x="112" y="344" class="metric-label">LARGEST STATE · GUJARAT</text><text x="572" y="321" class="metric">274.69 GW</text><text x="572" y="344" class="metric-label">ALL CATEGORIES IN MNRE TABLE</text><text x="84" y="410" class="section">CAPACITY MAP · GW</text><path d="${indiaPath}" fill="${lightGreen}" stroke="${green}" stroke-width="1.8"/>${bubbles}<text x="84" y="1094" class="small">Bubble size shows installed renewable capacity for selected high-capacity states.</text><text x="84" y="1120" class="small">The outline is the official India geometry bundled with VizBridge.</text><text x="628" y="410" class="section">TOP STATES · RANKED</text>${bars}<rect x="84" y="1168" width="912" height="1" fill="#cfe2d7"/><text x="84" y="1205" class="small">SOURCE · MNRE, Government of India · State-wise (location based) installed capacity of Renewable Power as on 31.03.2026</text><text x="84" y="1230" class="small">Verified source: ${esc(sourceUrl)}</text><text x="996" y="1266" text-anchor="end" class="kicker">VIZBRIDGE</text></svg>`;
await writeFile(new URL('../../../../outputs/india-renewable-power-map-2026.svg', import.meta.url), svg, 'utf8');
